databaseChangeLog:
  - changeSet:
      id: 0-create-admin-schema
      author: alex
      changes:
        - sql:
            sql: |
              CREATE SCHEMA IF NOT EXISTS app_admin AUTHORIZATION alex;

              GRANT ALL ON SCHEMA app_admin TO alex;

              ALTER DEFAULT PRIVILEGES IN SCHEMA app_admin
              GRANT ALL ON TABLES TO alex;